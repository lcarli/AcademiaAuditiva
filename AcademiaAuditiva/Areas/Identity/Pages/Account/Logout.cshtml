@page
@model LogoutModel
@{
    ViewData["Title"] = Localizer["Logout.Header"];
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">@Localizer["Logout.Confirmation"]</button>
            </form>
        }
        else
        {
            <p>@Localizer["Logout.Text"]</p>
        }
    }
</header>
