@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verificação com código de recuperação";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
    Você solicitou o login com um código de recuperação. Este login não será lembrado até que você forneça
    um código de aplicativo autenticador ao fazer login ou desative a autenticação de dois fatores e faça login novamente.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="Código de Recuperação" />
                <label asp-for="Input.RecoveryCode" class="form-label">Código de Recuperação</label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Entrar</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
