@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Redefinir chave de autenticador";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Se você redefinir a sua chave de autenticador, o seu aplicativo autenticador não funcionará até que você a reconfigure.</strong>
    </p>
    <p>
        Esse processo desabilita a autenticação de dois fatores até que você verifique o seu aplicativo autenticador.
        Se você não concluir a configuração do aplicativo autenticador, poderá perder o acesso à sua conta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Redefinir chave de autenticador</button>
    </form>
</div>
